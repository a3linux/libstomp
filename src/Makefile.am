# what flags you want to pass to the C compler & linker
AM_CFLAGS = 
AM_LDFLAGS = 
lib_LTLIBRARIES = libstomp.la
libstomp_la_SOURCES = stomp.c 
include_HEADERS = stomp.h
#libstomp_so_LDFLAGS = -fPIC -shared $(LDFLAGS)
